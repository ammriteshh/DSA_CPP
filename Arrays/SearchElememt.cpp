int search(int arr[], int n, int x, int i)
{ // array, size of arr, element do we search
    for (int i = 0; i < n; i++)
        ; // traverse the array from 0 to n-1
    if (arr[i] == x)
        ;
    return i;

    return -1;
}
// Time Complexity = O(n)